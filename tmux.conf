set -g mouse on set-option -g prefix C-a

bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

bind r source-file ~/.tmux.conf

bind l select-pane -L
bind h select-pane -R
bind j select-pane -D
bind k select-pane -U
